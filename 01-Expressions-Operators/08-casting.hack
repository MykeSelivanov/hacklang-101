// Casts in Hack convert values to different types. 
(float)1; // 1.0
(int)3.14; // 3, rounds towards zero

(bool)0; // false

(string)false; // ""

// Casts are only supported for bool, int, float and string.